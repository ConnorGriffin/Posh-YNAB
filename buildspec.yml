version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  install:
    commands:
        - apt-get install wget
        - wget https://dot.net/v1/dotnet-install.sh -O | sh
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
        - pwsh ./CodeDeploy.ps1 -Phase Build
        #
  #post_build:
    #commands:
      # - command
      # - command
#artifacts:
  #files:
    # - location
    # - location
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths
