version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  install:
    commands:
        - pwsh ./CodeDeploy.ps1 -Phase Install
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
        - pwsh ./CodeDeploy.ps1 -Phase Build
      # - command
      # - command
  #post_build:
    #commands:
      # - command
      # - command
#artifacts:
  #files:
    # - location
    # - location
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths
